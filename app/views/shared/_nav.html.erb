<nav class="navbar navbar-expand-lg bg-lightblue">
  <div class="container-fluid">
   <%= link_to "Home", groups_path, class: "nav-link fs-3 text-light" %>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
      <%= link_to "Home", root_path, class: "nav-link fs-5 text-light" %>
        <%= link_to "Category", groups_path, class: "nav-link fs-5 text text-light" %>
        <% if @group %>
    <%= link_to "All Expenses", group_items_path(@group), class: "nav-link fs-5 text-light" %>
  <% end %>
        <%= button_to "Logout".html_safe,
        destroy_user_session_path,
        method: :delete,
        class: "nav-link fs-5 text-light" %>

      </ul>
    </div>
  </div>
</nav>